% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_dep_graph.R
\name{bw_diamonds}
\alias{bw_diamonds}
\title{Find diamond (shared) dependencies in a graph}
\usage{
bw_diamonds(graph)
}
\arguments{
\item{graph}{A \code{bw_depgraph}/\code{igraph} from \code{\link[=build_dep_graph]{build_dep_graph()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{package}, \code{n_dependents},
and \code{dependents} (a list column), ordered by \code{n_dependents} descending.
}
\description{
A diamond dependency is a package required by two or more other packages.
These are exactly the places where version conflicts arise, so they are the
first thing to inspect when an install misbehaves.
}
\examples{
bw_diamonds(build_dep_graph(bw_example("clean.lock")))
}
