% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dashboard.R
\name{run_dashboard}
\alias{run_dashboard}
\title{Launch the interactive buildwright dashboard}
\usage{
run_dashboard(
  x = NULL,
  platform = bw_host_platform(),
  host_libraries = NULL,
  launch.browser = interactive(),
  port = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{bw_library}, a \code{bw_diagnosis}, a path \code{\link[=scan_library]{scan_library()}} accepts,
or \code{NULL} to load the bundled example lockfile.}

\item{platform, host_libraries}{Forwarded to \code{\link[=diagnose]{diagnose()}}.}

\item{launch.browser}{Logical; open a browser. Defaults to \code{interactive()}.}

\item{port}{Optional port for \code{\link[shiny:runApp]{shiny::runApp()}}.}

\item{...}{Additional arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
The value returned by \code{\link[shiny:runApp]{shiny::runApp()}}, invisibly. Called for its
side effect of running the app.
}
\description{
Starts the bundled Shiny application (\code{inst/shiny/app.R}) showing the
dependency DAG, the conflict table, the system-requirement checklist, and a
"will this install?" simulator. The diagnosis is computed up front and handed
to the app through a session option, so the app starts instantly and works
offline.
}
\examples{
\dontrun{
run_dashboard(bw_example("conflicted.lock"))
}
}
\seealso{
\code{\link[=report]{report()}}, \code{\link[=diagnose]{diagnose()}}
}
