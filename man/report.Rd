% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report}
\alias{report}
\title{Render a buildwright report or launch the dashboard}
\usage{
report(
  x,
  format = c("quarto", "shiny"),
  output = NULL,
  platform = bw_host_platform(),
  host_libraries = NULL,
  open = interactive(),
  quiet = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{bw_library}, a \code{bw_diagnosis}, or a path \code{\link[=scan_library]{scan_library()}} accepts.}

\item{format}{\code{"quarto"} (static HTML report) or \code{"shiny"} (dashboard).}

\item{output}{For the quarto/HTML report, the output file path. Defaults to a
temp file. Ignored for the shiny format.}

\item{platform, host_libraries}{Forwarded to the diagnosis.}

\item{open}{Logical; open the rendered report / launch the browser when
interactive.}

\item{quiet}{Logical; suppress rendering chatter.}

\item{...}{Passed to \code{\link[=run_dashboard]{run_dashboard()}} for the shiny format.}
}
\value{
For \code{"quarto"}, the path to the rendered HTML (invisibly). For
\code{"shiny"}, the result of \code{\link[=run_dashboard]{run_dashboard()}}.
}
\description{
Two output formats share one entry point:
\itemize{
\item \code{format = "quarto"} renders the parameterised report shipped at
\code{inst/report/buildwright_report.qmd} to a self-contained HTML file. If the
Quarto CLI is not available, buildwright transparently falls back to an
equivalent R Markdown template (\code{buildwright_report.Rmd}) rendered with
\code{\link[rmarkdown:render]{rmarkdown::render()}}, so you still get an HTML artifact.
\item \code{format = "shiny"} launches the interactive dashboard via
\code{\link[=run_dashboard]{run_dashboard()}}.
}
}
\examples{
\dontrun{
# Static HTML report from a lockfile
report(bw_example("conflicted.lock"), format = "quarto",
       output = "buildwright_report.html")

# Interactive dashboard
report(bw_example("conflicted.lock"), format = "shiny")
}
}
\seealso{
\code{\link[=diagnose]{diagnose()}}, \code{\link[=run_dashboard]{run_dashboard()}}
}
