% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-data.R
\name{bw_graph_data}
\alias{bw_graph_data}
\title{Convert a dependency graph to node/edge data frames}
\usage{
bw_graph_data(graph)
}
\arguments{
\item{graph}{A \code{bw_depgraph}/\code{igraph} from \code{\link[=build_dep_graph]{build_dep_graph()}}.}
}
\value{
A list with two \link[tibble:tibble]{tibbles}:
\itemize{
\item \code{nodes}: \code{id}, \code{label}, \code{group} (source), \code{present}, \code{title} (HTML
tooltip), \code{color}.
\item \code{edges}: \code{from}, \code{to}, \code{constraint}, \code{arrows}.
}
}
\description{
Produces the \code{nodes} and \code{edges} data frames used by the Shiny dashboard
(and anything else that wants to draw the DAG, e.g. \code{visNetwork}). Nodes are
grouped and coloured by source, with referenced-but-missing packages
highlighted.
}
\examples{
gd <- bw_graph_data(build_dep_graph(bw_example("clean.lock")))
gd$nodes
}
