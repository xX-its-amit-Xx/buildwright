% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_library.R
\name{scan_library}
\alias{scan_library}
\alias{bw_example}
\title{Scan an R library or \code{renv} lockfile into a package inventory}
\usage{
scan_library(path = NULL, include_base = FALSE)

bw_example(name = c("clean.lock", "conflicted.lock", "missing-sysreq.lock"))
}
\arguments{
\item{path}{Path to an \code{renv.lock} file, a project/library directory, or
\code{NULL} to scan the current library.}

\item{include_base}{Logical; include base-priority packages that ship with R.
Defaults to \code{FALSE} because they are always available and only add noise.}

\item{name}{Fixture name shipped with the package.}
}
\value{
A \link[tibble:tibble]{tibble} of class \code{bw_library} with one row per
package and the columns:
\describe{
\item{package}{Package name.}
\item{version}{Resolved version string.}
\item{source}{Canonical source: \code{"CRAN"}, \code{"Bioconductor"}, \code{"GitHub"},
\code{"local"}, \code{"Repository"}, \code{"base"}, or \code{"Unknown"}.}
\item{repository}{Human-readable origin (e.g. \code{"CRAN"}, \code{"user/repo@sha"}).}
\item{requirements}{List column of direct dependency strings, possibly
version-qualified (e.g. \code{"rlang (>= 1.0.0)"}).}
\item{is_base}{Whether the package ships with R.}
\item{priority}{Install priority for library scans (\code{NA} for lockfiles).}
}
Useful metadata is attached as attributes: \code{bw_type} (\code{"lockfile"} or
\code{"library"}), \code{bw_input}, \code{bw_r_version}, and \code{bw_bioc_version}.

\code{bw_example()} returns the file path to a bundled fixture lockfile.
}
\description{
\code{scan_library()} is the entry point for every other buildwright function. It
reads either an installed library (a directory of installed packages) or an
\code{renv.lock} file and returns a tidy inventory of packages, their versions,
where they came from (CRAN / Bioconductor / GitHub / local / custom
repository), and their direct requirements.
}
\details{
The input is auto-detected:
\itemize{
\item \code{NULL} (the default) scans the first library on \code{.libPaths()}.
\item A path to an \code{renv.lock} (or any \verb{*.lock} / \verb{*.json}) is parsed as a
lockfile.
\item A directory containing an \code{renv.lock} is parsed as a lockfile.
\item Any other existing directory is treated as an installed library and scanned
with \code{\link[utils:installed.packages]{utils::installed.packages()}}.
}
}
\examples{
lib <- scan_library(bw_example("clean.lock"))
lib
nrow(lib)
table(lib$source)
bw_example("conflicted.lock")
}
\seealso{
\code{\link[=build_dep_graph]{build_dep_graph()}}, \code{\link[=detect_conflicts]{detect_conflicts()}}, \code{\link[=diagnose]{diagnose()}}
}
