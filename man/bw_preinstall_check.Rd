% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hooks.R
\name{bw_preinstall_check}
\alias{bw_preinstall_check}
\title{Pre-install health gate for renv / pak workflows}
\usage{
bw_preinstall_check(
  lockfile = "renv.lock",
  strict = FALSE,
  platform = bw_host_platform(),
  host_libraries = NULL,
  quiet = FALSE
)
}
\arguments{
\item{lockfile}{Path to an \code{renv.lock}, a library/project directory, a
\code{bw_library}, or \code{NULL} for the current library.}

\item{strict}{Logical; if \code{TRUE}, abort (error) when conflicts, predicted
install failures, or missing system libraries are found. If \code{FALSE}
(default), report and return without erroring.}

\item{platform, host_libraries}{Passed to \code{\link[=diagnose]{diagnose()}}.}

\item{quiet}{Logical; suppress the printed summary.}
}
\value{
The \code{bw_diagnosis} object, invisibly. Called mainly for its gate
(error under \code{strict}) and printed summary.
}
\description{
Runs a full \code{\link[=diagnose]{diagnose()}} over a lockfile (or library) and, when \code{strict = TRUE}, raises an error if any problems are predicted. This is designed to sit
\emph{in front of} an install step so a broken dependency set fails fast instead
of part-way through building a few hundred packages.
}
\details{
Typical wiring:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# In a restore script, gate renv::restore() on a clean bill of health:
buildwright::bw_preinstall_check("renv.lock", strict = TRUE)
renv::restore()

# As a CI step (non-zero exit fails the job):
# Rscript -e 'buildwright::bw_preinstall_check("renv.lock", strict = TRUE)'

# Before a pak install, on a host where you know the libraries present:
bw_preinstall_check("renv.lock", strict = TRUE,
                    host_libraries = c("libcurl", "openssl"))
pak::pkg_install(...)
}\if{html}{\out{</div>}}
}
\examples{
# Non-strict: never errors, just reports.
invisible(bw_preinstall_check(bw_example("clean.lock")))

# Strict on a healthy lockfile returns silently; on a broken one it errors:
bw_preinstall_check(bw_example("clean.lock"), strict = TRUE, quiet = TRUE)
tryCatch(
  bw_preinstall_check(bw_example("conflicted.lock"), strict = TRUE, quiet = TRUE),
  error = function(e) cat("gate tripped:", conditionMessage(e), "\n")
)
}
\seealso{
\code{\link[=diagnose]{diagnose()}}, \code{\link[=simulate_install]{simulate_install()}}
}
