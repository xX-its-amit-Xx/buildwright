% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_dep_graph.R
\name{build_dep_graph}
\alias{build_dep_graph}
\title{Build a dependency graph from a library or lockfile}
\usage{
build_dep_graph(x, include_base = FALSE)
}
\arguments{
\item{x}{A \code{bw_library} (from \code{\link[=scan_library]{scan_library()}}), or a path that
\code{\link[=scan_library]{scan_library()}} understands.}

\item{include_base}{Logical; include edges to base/recommended packages that
ship with R. Defaults to \code{FALSE} to keep the DAG focused on the
distribution under management.}
}
\value{
An \code{igraph} object of class \code{bw_depgraph}. Vertex attributes:
\code{version}, \code{source}, \code{present}, \code{is_base}. Edge attribute: \code{constraint}
(the raw requirement string, e.g. \code{">= 1.0.0"} or \code{""}).
}
\description{
Constructs a directed \link[igraph:igraph]{igraph} dependency graph where an edge
\code{A -> B} means "package A requires package B". Vertices carry the package
version, source and whether the package is actually present in the scanned
set, so that missing dependencies show up as vertices with \code{present = FALSE}.
The returned object also gains the class \code{bw_depgraph} for a richer print
method, while remaining a fully functional \code{igraph} object.
}
\examples{
g <- build_dep_graph(bw_example("clean.lock"))
g
igraph::vcount(g)
bw_cycles(g)
}
\seealso{
\code{\link[=bw_cycles]{bw_cycles()}}, \code{\link[=bw_diamonds]{bw_diamonds()}}, \code{\link[=detect_conflicts]{detect_conflicts()}}
}
