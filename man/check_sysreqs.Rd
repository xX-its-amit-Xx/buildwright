% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_sysreqs.R
\name{check_sysreqs}
\alias{check_sysreqs}
\title{Map packages to system requirements and flag likely-missing libraries}
\usage{
check_sysreqs(
  x,
  platform = bw_host_platform(),
  host_libraries = NULL,
  use_pak = FALSE
)
}
\arguments{
\item{x}{A \code{bw_library} or a path \code{\link[=scan_library]{scan_library()}} accepts.}

\item{platform}{One of \code{"linux"}, \code{"macos"}, \code{"windows"}; defaults to the
host. Controls which install hint is emitted.}

\item{host_libraries}{Optional character vector of canonical library tokens
known to be present on the target host (e.g. \code{c("libcurl", "openssl")}).}

\item{use_pak}{Logical; if \code{TRUE} and \code{pak} is installed, query
\code{\link[pak:pkg_sysreqs]{pak::pkg_sysreqs()}} for an authoritative mapping (requires network).}
}
\value{
A \link[tibble:tibble]{tibble} of class \code{bw_sysreqs} with one row per
(package, system library): \code{package}, \code{system_requirement}, \code{library},
\code{platform}, \code{present} (logical or \code{NA}), \code{status} (\code{"ok"}/\code{"missing"}/
\code{"unknown"}), \code{install_hint}, and \code{source}. Packages with no known system
requirements are omitted.
}
\description{
Translates each R package into the external system libraries it needs (the
\code{SystemRequirements} you only discover when a source install explodes with
"cannot find -lxml2"). Mappings come from three places, in order: the
package's own \code{SystemRequirements} field (for installed-library scans), an
optional authoritative lookup via \code{pak}, and a curated offline table
(\code{\link[=bw_sysreq_package_map]{bw_sysreq_package_map()}}).
}
\details{
Whether a library is \emph{present} is resolved as follows: if \code{host_libraries}
is supplied, presence is membership in that set (deterministic, ideal for CI
and tests); otherwise on Linux buildwright probes \code{pkg-config}, and on other
platforms presence is reported as unknown (\code{NA}) because CRAN binaries bundle
their libraries.
}
\examples{
# Pretend only libcurl is installed on the target host:
check_sysreqs(bw_example("missing-sysreq.lock"),
              platform = "linux", host_libraries = "libcurl")
}
\seealso{
\code{\link[=simulate_install]{simulate_install()}}, \code{\link[=bw_sysreq_package_map]{bw_sysreq_package_map()}}
}
