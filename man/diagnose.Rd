% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{diagnose}
\alias{diagnose}
\title{Run the full buildwright diagnosis in one call}
\usage{
diagnose(
  x,
  platform = bw_host_platform(),
  host_libraries = NULL,
  use_pak = FALSE
)
}
\arguments{
\item{x}{A \code{bw_library} or a path \code{\link[=scan_library]{scan_library()}} accepts.}

\item{platform, host_libraries, use_pak}{Passed to \code{\link[=check_sysreqs]{check_sysreqs()}} and
\code{\link[=simulate_install]{simulate_install()}}.}
}
\value{
An object of class \code{bw_diagnosis}: a list with elements \code{library},
\code{graph}, \code{conflicts}, \code{sysreqs}, \code{plan}, and \code{meta}.
}
\description{
Convenience wrapper that runs \code{\link[=scan_library]{scan_library()}}, \code{\link[=build_dep_graph]{build_dep_graph()}},
\code{\link[=detect_conflicts]{detect_conflicts()}}, \code{\link[=check_sysreqs]{check_sysreqs()}} and \code{\link[=simulate_install]{simulate_install()}} over a
single input and bundles the results. This is what \code{\link[=report]{report()}} and
\code{\link[=run_dashboard]{run_dashboard()}} consume, and the most ergonomic entry point for scripting a
pre-install health check.
}
\examples{
d <- diagnose(bw_example("conflicted.lock"))
d
d$conflicts
}
