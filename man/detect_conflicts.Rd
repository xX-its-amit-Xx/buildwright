% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_conflicts.R
\name{detect_conflicts}
\alias{detect_conflicts}
\title{Detect unsatisfiable dependency constraints}
\usage{
detect_conflicts(x)
}
\arguments{
\item{x}{A \code{bw_library}, a \code{bw_depgraph}, or a path \code{\link[=scan_library]{scan_library()}} accepts.}
}
\value{
A \link[tibble:tibble]{tibble} of class \code{bw_conflicts}, one row per
problem, with columns \code{package}, \code{type}, \code{required_by} (list column),
\code{constraints}, \code{installed_version}, \code{satisfiable}, and \code{detail}. A healthy
library yields a zero-row tibble.
}
\description{
Walks every declared requirement and reports the packages whose constraints
cannot be jointly satisfied. Four kinds of problem are surfaced:
}
\details{
\itemize{
\item \strong{missing} -- a required package is absent from the scanned set and is not
a base package, so nothing can depend on it successfully.
\item \strong{version_conflict} -- two or more packages demand version ranges of the
same dependency whose intersection is empty (no single version works).
\item \strong{version_drift} -- a feasible range exists, but the version actually
present (or pinned in the lockfile) falls outside it.
\item \strong{cycle} -- packages form a dependency cycle, so no install order exists.
}

Version-range checks need version-qualified requirements. Installed-library
scans always carry them (parsed from \code{DESCRIPTION}); \code{renv.lock} scans carry
them only when the lockfile records them, so for plain lockfiles this focuses
on missing dependencies, cycles and diamonds.
}
\examples{
detect_conflicts(bw_example("clean.lock"))
detect_conflicts(bw_example("conflicted.lock"))
}
\seealso{
\code{\link[=build_dep_graph]{build_dep_graph()}}, \code{\link[=simulate_install]{simulate_install()}}
}
