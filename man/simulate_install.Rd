% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_install.R
\name{simulate_install}
\alias{simulate_install}
\title{Simulate an installation and predict failures before running it}
\usage{
simulate_install(
  x,
  installed = NULL,
  platform = bw_host_platform(),
  host_libraries = NULL,
  use_pak = FALSE
)
}
\arguments{
\item{x}{A \code{bw_library} or a path \code{\link[=scan_library]{scan_library()}} accepts.}

\item{installed}{Character vector of packages already present on the target
(treated as satisfied). Defaults to base/recommended packages.}

\item{platform, host_libraries, use_pak}{Passed to \code{\link[=check_sysreqs]{check_sysreqs()}}.}
}
\value{
A list of class \code{bw_install_plan} with elements \code{order} (character),
\code{steps} (tibble: \code{step}, \code{package}, \code{version}, \code{source},
\code{predicted_status}, \code{reason}), \code{predicted_failures} (the failing/blocked
subset), \code{feasible} (logical), \code{has_cycle} (logical), and the counts
\code{n_ok}, \code{n_warn}, \code{n_fail}.
}
\description{
Produces a dependency-first (topological) install order and, for every
package, a predicted outcome based on the conflicts and system requirements
buildwright found. Failures propagate: if a dependency cannot be installed,
every package that (transitively) needs it is marked \code{blocked}. This is the
dry run you want before letting \code{renv}/\code{pak} actually build a few hundred
packages on a fresh machine.
}
\details{
Predicted statuses:
\itemize{
\item \code{ok} -- expected to install.
\item \code{warn} -- a system library looks missing, or the pinned version drifts
from a feasible range; the install may still work but is risky.
\item \code{fail} -- directly unsatisfiable (version conflict, in a cycle, or a hard
dependency is missing).
\item \code{blocked} -- depends, transitively, on something that fails.
}
}
\examples{
plan <- simulate_install(bw_example("clean.lock"))
plan
plan$order

simulate_install(bw_example("conflicted.lock"))$predicted_failures
}
\seealso{
\code{\link[=detect_conflicts]{detect_conflicts()}}, \code{\link[=check_sysreqs]{check_sysreqs()}}, \code{\link[=diagnose]{diagnose()}}
}
